#!/usr/bin/env bash
# This script checks if a host is reachable by pinging the given IP address 5 times.

# Check if an argument is provided
if [ "$#" -ne 1 ]; then
    echo "Usage: 5-is_the_host_on_the_network {IP_ADDRESS}"
    exit 1
fi

# Ping the IP address 5 times
ping -c 5 "$1"
